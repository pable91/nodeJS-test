mixin movieAll(movie = {})
    .movieBlock 
        a(href=`/${movie.id}`)
            p=movie.title


mixin movieDetail(movie = {})
    h1= movie.title
        h4= movie.summary
        ul.list 
            each genre in movie.genres
                li= genre